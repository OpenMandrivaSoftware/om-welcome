#!/bin/sh
konsole -e pkexec sh -c "dnf clean all ; dnf repolist ; dnf distro-sync --refresh --allowerasing 2>&1 | tee $HOME/$(date -I)-dsync-log.txt ; chown $USER:$USER $HOME/*dsync-log.txt ; echo 'Press enter to close konsole'; read"
